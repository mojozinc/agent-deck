[package]
name = "agent-deck-daemon"
version = "0.1.0"
edition = "2021"

[dependencies]
agent-deck-core = { path = "../agent-deck-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"
tokio = { version = "1", features = ["full"] }
walkdir = "2.5"
